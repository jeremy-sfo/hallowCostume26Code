//#include <Arduino.h>
//#include <U8g2lib.h>
//#include <Wire.h>

// Software I2C Constructor for NodeMCU Boards with Integrated OLED
// Rotation: U8G2_R0 (Normal landscape orientation)
// Clock (SCL): Pin 14 (NodeMCU D5)
// Data (SDA): Pin 12 (NodeMCU D6)
// Reset: U8X8_PIN_NONE (Onboard OLEDs share the system reset)
//U8G2_SSD1306_128X64_NONAME_F_SW_I2C u8g2(U8G2_R0, /* clock=*/ 14, /* data=*/ 12, /* reset=*/ U8X8_PIN_NONE);

//void setup(void) {
  //u8g2.begin(); // Initialize the onboard OLED display
//}

//void loop(void) {
  //u8g2.clearBuffer();					// Clear the internal memory buffer
 // u8g2.setFont(u8g2_font_7x14B_tr);	// Choose a suitable scannable font
  //u8g2.drawStr(0, 15, "NodeMCU OLED");  // Write text at X=0, Y=15
 // u8g2.drawStr(0, 35, "U8g2 Setup OK!"); // Write text at X=0, Y=35
  //u8g2.sendBuffer();					// Push the buffer data to the screen
  
  //delay(1000); 
//}*/
